// import { FilterError, SearchFilter } from "@/components/queryBar/types";
// import { LogsAssetCache } from "../types";
// import { getProtocolValueSuggestions } from "./suggestions";

// type Result = FilterError | undefined;

// export function validateProtocolValues(searchFilter: SearchFilter, _assetCache: LogsAssetCache): Result {
//   const suggestions = getProtocolValueSuggestions(searchFilter);

//   if (!suggestions.some((item) => item.value == searchFilter.value))
//     return {
//       field: "value",
//       message: "Invalid protocol",
//     };
// }
